[package]
name = "biosphere_py"
version = "0.3.0"
edition = "2021"

[lib]
name = "biosphere"
crate-type = ["cdylib"]

[package.metadata.maturin]
name = "biosphere"

[dependencies]
numpy = "0.17"
biosphere = { path = "../" }
ndarray = "0.15"
pyo3 = {version = "0.17", features = ["extension-module"]}