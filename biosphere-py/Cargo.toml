[package]
name = "biosphere_py"
version = "0.2.0"
edition = "2018"

[lib]
name = "biosphere"
crate-type = ["cdylib"]

[package.metadata.maturin]
name = "biosphere"

[dependencies]
numpy = "0.15"
biosphere = { path = "../" }
ndarray = "0.15"
pyo3 = {version = "0.15", features = ["extension-module"]}